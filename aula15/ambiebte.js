var valores = [2, 8, 6, 4]

/*for (let seq = 0; seq < valores.length; seq++){
    valores.sort()
    console.log(' Os valores são ' + valores[seq])
}*/

for(seq in valores) {
    valores.sort()
    console.log(' Os valores são este ai ' + valores[seq])
}